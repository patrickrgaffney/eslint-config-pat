// Example file linted using the "pat/react" config.
import React from 'react'
import { render } from 'react-dom'

const root = (
  <div>
    <p>some text here</p>
  </div>
)

render(root, document.getElementById('main'))
